lower_suramar_barrier_decision = {
    # By Dione
    ai_check_interval = 45
	picture = {
		reference = "gfx/interface/illustrations/decisions/lift_suramar_barrier.dds"
	}
	decision_group_type = major

    is_shown = {
        NOT = { has_global_variable = suramar_barrier_lowered_variable }
        has_title = title:k_suramar
        is_independent_ruler = yes
        has_character_flag = had_outsider_event
    }

    is_valid = {
        legitimacy_level >= 3
        #TODO - add hook-in for discovering outside world
    }

    is_valid_showing_failures_only = {
		is_imprisoned = no
		is_capable_adult = yes
		is_at_war = no
	}

    effect = {
        custom_tooltip = lower_suramar_barrier_decision_tt
        trigger_event = isolation.3
        custom_tooltip = {
            text = lower_suramar_barrier_decision_tt_modifier
            every_sub_realm_county = {
                limit = {
                    has_county_modifier = wc_suramar_isolated_modifier
                }
                remove_county_modifier = wc_suramar_isolated_modifier
            }
        }
    }

    ai_potential = {
		OR = {
            current_date >= 615.1.1
            has_character_flag = had_outsider_event
        }
	}

	ai_will_do = {
		base = 10
        modifier = {
            add = 100
            current_date >= 615.1.1
            #TODO_WARCRAFT add more features for legion 3rd invasion
        }
	}
}